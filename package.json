{
  "name": "francis",
  "version": "0.1.0",
  "description": "A robust ajax wrapper (currently using jquery.ajax) which handles the concept of transactions which could potentially include multiple server calls and allows to bind a context for an entire transaction.",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/amhester/Francis.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/amhester/Francis/issues"
  },
  "homepage": "https://github.com/amhester/Francis#readme",
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-clean": "^0.3.2",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^1.5.3"
  }
}
